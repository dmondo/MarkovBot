describe('MongoDB', () => {
  it('should persist user timelines in database', () => {

  });

  it('should use cached model from database instead of creating a new model every time', () => {

  });
});
