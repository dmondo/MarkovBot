describe('history component', () => {
  it('should render', () => {

  });

  it('should return a div element', () => {

  });

  it('should call useEffect on first render only', () => {

  });

  it('should fetch existing saved twerts on first render', () => {

  });
});
